services:
  wg-easy-v15:
    container_name: wg-easy-v15
    image: ghcr.io/wg-easy/wg-easy:15.2
    restart: unless-stopped
    privileged: false
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    networks:
      - wg-easy-v15
    ports:
      - mode: ingress
        target: 51820
        published: "51820"
        protocol: udp
      - mode: ingress
        target: 51821
        published: "51821"
        protocol: tcp
    volumes:
      - type: bind
        source: /DATA/AppData/wg-easy-v15/data
        target: /etc/wireguard
        bind:
          create_host_path: true
    sysctls:
      net.ipv4.conf.all.src_valid_mark: "1"
      net.ipv4.ip_forward: "1"
      net.ipv6.conf.all.disable_ipv6: "0"
      net.ipv6.conf.all.forwarding: "1"
      net.ipv6.conf.default.forwarding: "1"
    environment:
      - INIT_DNS=9.9.9.9,149.112.112.112
      - INIT_ENABLED=true
      - INIT_HOST=https://remote-access-home-34.asuscomm.com
      - INIT_IPV4_CIDR=10.8.0.0/24
      - INIT_IPV6_CIDR=fd42:42:42::/64
      - INIT_PASSWORD=password  # (1)!
      - INIT_PORT=51820
      - INIT_USERNAME=admin
      - INSECURE=true

networks:
  wg-easy-v15:
    name: wg-easy-v15